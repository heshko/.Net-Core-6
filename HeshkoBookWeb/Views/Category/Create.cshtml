@model CategoryDTO
@{
    ViewData["Title"] = "Create Category";
}


<br />
<div class="container mt-4">
    <h2 class="text-info">Create Category</h2>
    <div class="row">
      <div asp-validation-summary="All" class="text-danger"></div>
        <form class="col-6 border p-3 m-3 " method="post"  >
            <div class="mb-3">
              <label asp-for="Name" class="form-label">Category Name</label>
              <input type="text" class="form-control" asp-for="Name" placeholder="Name">
               <span class="text-danger" asp-validation-for="Name"></span>
            </div>
            <div class="mb-3">
              <label  asp-for="DisplayOrder" class="form-label">Display Order</label>
              <input  class="form-control" asp-for="DisplayOrder" placeholder="Display Order">
              <span class="text-danger" asp-validation-for="DisplayOrder"></span>
            </div>
            <button class="btn-info" type="submit">Create</button>
        </form>
    </div>
    <a class="btn btn-warning" asp-area="" asp-controller="Category" asp-action="Index"   >Back To Category List</a>
</div>

<!--Its for i want to movie validation from server side to client side notice its will work only with dataannotation validation custom validation it will not work like 
     if (categoryDTO.Name == categoryDTO.DisplayOrder.ToString())
            {
                ModelState.AddModelError("name", "The name of category and the display oredr shouldnt have the same name");
            }
            it will not come to my category  controll ! 
-->
@section Scripts
{
@{
    <partial name="_ValidationScriptsPartial" />
}
}